# Lidera Flow (PLL) — slot do Æxodo

**Status:** Não havia pasta legada `lideraflow` no monorepo no momento da migração (mar/2026). O produto **Lidera Flow (PLL)** está referenciado no backlog interno; quando o código for incorporado, coloque-o **neste diretório** (`apps/clientes/01_lidera/lidera/flow`) e preencha o `package.json` com scripts reais.

O pacote `@cliente/lidera-flow` existe apenas para reservar o workspace pnpm e o caminho canônico.
