# manifests/ (fase 2)

Reservado para **schemas leves** (`agents.yaml`, `mcps.yaml`, …) que permitam gerar ou validar partes do `INDEX.md` / `docs/COLEÇÃO_DOCS_PARA_IA.md` sem drift.

- **Estado:** stub — nenhum manifest obrigatório ainda.
- **Validação:** quando existirem ficheiros YAML, alinhar com [`tools/scripts/validate-registry-links.sh`](../../../tools/scripts/validate-registry-links.sh) ou script dedicado.
- **WorkOS futuro:** o Admin pode consumir cópia indexada destes manifestos + `INDEX.md` (RAG, permissões).
