# diagrams/

Coloque aqui exports **Mermaid** (`.mmd`), PNG/SVG ou notas que a wiki referencia — sempre como **complemento** ao texto em `INDEX.md` e no plano Adventure OS.

## Processo Adventure OS (FigJam gerado a partir destes `.mmd`)

| Arquivo | Conteúdo |
|---------|----------|
| [`processo-adventure-os-hoje.mmd`](processo-adventure-os-hoje.mmd) | Fluxo **estado atual**: Asana → Grove/INDEX → knowledge/docs/apps → engenharia (BACKLOG/PR) + WorkOS/n8n. |
| [`processo-adventure-os-apos-plano.mmd`](processo-adventure-os-apos-plano.mmd) | Fluxo **alvo pós-plano**: inbox WorkOS opcional, CI no registry, RAG, distribuição explícita, ADRs, auditoria, gate cultural. |

Para **recriar ou atualizar** o quadro no FigJam: colar o Mermaid no FigJam (Mermaid widget) ou usar a integração que gera o diagrama a partir da sintaxe. Links de *claim* gerados pela API expiram em ~7 dias; o `.mmd` no repo é a fonte estável.

**IDs de diagramas gerados (referência):** `hoje` → `2d8fa0db-511f-41f3-a66f-5b8b44a3d2e8` · `alvo` → `ca759c78-9a78-4f33-aa65-51bcb5e29728`.
