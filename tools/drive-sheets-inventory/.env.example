# Caminho para OAuth Desktop (JSON do Google Cloud Console). Não commitar.
GOOGLE_CREDENTIALS_PATH=./credentials.json
# Token gerado no primeiro login (contato@adventurelabs.com.br). Não commitar.
GOOGLE_TOKEN_PATH=./token.json
# Opcional: após primeira criação, fixar a planilha de controle para só atualizar
# DRIVE_SHEETS_INVENTORY_SPREADSHEET_ID=

# Opcional: chave Google AI Studio / Gemini — só para --gemini-descriptions
# GEMINI_API_KEY=
# GEMINI_MODEL=gemini-2.0-flash
